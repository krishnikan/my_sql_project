========================================================================================
                          find one student mark when input student student_id
========================================================================================
 
 delimiter $$
  create procedure getstumark(in input_stuid varchar(15))
     begin
     select * from student_final_grade
     where student_id = input_stuid;
     end $$

delimiter ;